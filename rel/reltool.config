{sys, [
    {lib_dirs, ["../apps", "../deps"]},
    {rel, "poxy", "0.1.0", [
        kernel,
        stdlib,
        lager,
        cowboy,
        poxy
    ]},
    {rel, "start_clean", "", [
        kernel,
        stdlib
    ]},
    {boot_rel, "poxy"},
    {profile, embedded},
    {excl_sys_filters, [
        "^bin/.*",
        "^erts.*/bin/(dialyzer|typer)"
    ]},
    {app, poxy, [{incl_cond, include}]}
]}.

{target_dir, "package"}.

{overlay, [
   {mkdir, "log/sasl"},
   {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
   {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},
   {copy, "files/poxy", "bin/poxy"},
   {copy, "files/app.config", "etc/app.config"},
   {copy, "files/vm.args", "etc/vm.args"}
]}.
